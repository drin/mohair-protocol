version: v2
modules:
  - path: .
    name: buf.build/octalene/mohair
lint:
  use:
    - STANDARD
  ignore_only:
    PACKAGE_VERSION_SUFFIX:
      - mohair
breaking:
  use:
    - FILE
deps:
  - buf.build/octalene/substrait
